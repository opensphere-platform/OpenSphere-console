# Native commands

`cmd/os-cli` is the target source boundary for the Console-native `os` command. The current implementation remains under `backend/os-cli` until its device-flow, credential-store, update-trust, and command contracts pass the new package boundary.
