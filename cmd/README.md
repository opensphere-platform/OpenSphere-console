# Native commands

`cmd/os-cli` is the source boundary for the Console-native `os` command and its independently served download artifacts. The implementation moved here after its device-flow, credential-store, update-trust, manifest-signing, and command contracts passed the package boundary.
