/**
 * 검색 계약의 SSOT = @opensphere/console-contracts (C2 context).
 *
 * Fresh-start baseline — sibling source dependency를 제거하고 versioned contract package로 재구성했다.
 */
export type { SearchResult, SearchProvider } from '@opensphere/console-contracts';
