# Environment overlays

Each environment overlay declares values through a validated schema. An overlay may bind names, resources, topology, and public routes; it cannot broaden component ownership or bypass the integrated release BOM.
